// Notacion

//; -> Delimitar el final de una linea
const b = 4;
b + 4;

//. -> se utiliza en los objetos para acceder a los atributos
//movil.anchura

//[] -> Lista, arreglos, o arrays
const arr = [1, 2, 3, 4]
console.log(arr[2]);

//() -> funciones 
function suma(a, b) {
    //Se escribe la funcion
}


//{} -> Llaves para objetos, funciones y estrcutura de control
const movil = {
    anchura: 5,
    altura: 10
}